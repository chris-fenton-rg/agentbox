# Changesets

This directory tracks pending package version changes via [changesets](https://github.com/changesets/changesets).

To record a change, run:

```sh
pnpm changeset
```

Pick the affected packages, pick semver bumps, and write a short summary. Commit the generated markdown file alongside your code change.
